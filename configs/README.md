# 配置

`interaction/` 提供已检查参数名和路径解析的 JSON 配方。`train`/`infer` 示例文件属于 interaction 路线，不代表全项目默认模型。

其他路线使用 `python multiview.py run ROUTE -- --help` 查询原始参数，按 [项目路线图](../docs/PROJECT_MAP.md) 配置输入。旧服务器上的固定日期启动器、watcher 和任务授权配置留在原备份。

`*.local.json` 不提交 Git。训练参数示例不是质量已经复现的实验配方。
